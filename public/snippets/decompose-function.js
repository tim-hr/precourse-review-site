const renderColumnSums = function() {
  // for each col index up to the num of cols
  //    generate array of all numeric values in that col: filterNumbericValues(col)
  //    sum up values in the numeric array
  //    set footer element for that col to sum: setSumEl(col)
};

const filterNumericValues = function(col) {
  // fetch all cell values for that column into values array
  // for each value in the values array
  //    pass into result array if value is numeric
  // return the result array
};

const setSumEl = function(col) {
  // find footer element for col
  // set text content to sum
};