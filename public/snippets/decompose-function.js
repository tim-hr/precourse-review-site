const renderColumnSums = function() {
  // for each col index up to the num of cols
    // declare a new sum variable to 0
    // fetch the cell values for that column
    // filter all values through the getValue function
    // add up the filtered numeric values
    // run renderFooter with final sum of the column values
};

const getValue = function() {
  // filter out non-numeric values
    // non-numeric values return 0
};

const renderFooter = function() {
  // append all data elements with the sum to a new row
  // append the row to the footer element
};