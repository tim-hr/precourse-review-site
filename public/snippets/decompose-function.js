const renderColumnSums = function() {
  // for each col index up to the num of cols - 1
  // render new cell at ColIndex, SumRow
  // with value of computeSum for the column
};

const computeSum = function() {
  // sum colNumericValues
};

const createNumericArrayFromColumn = function() {
  // for each row index (cell) within given column
  // test whether cell value is numeric
  // if numeric, push cell value to array
  // return colNumericValues (array)
};


// Could also set bottom function to simply aggregate the values
// then set the middle function to filter and then sum