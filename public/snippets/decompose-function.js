const fetchCellValues = function() {
  // fetch all the cell values for that column from the model returning a list
};

const filterAndSum = function() {
  // filter out non-numeric values and sum them returning the sum
};

const renderColumnSums = function() {
  // find the footer element for this column and set its text content to the sum
};
