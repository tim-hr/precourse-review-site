const renderColumnSums = function() {
  // iterate through columns of table 
   //calculateSumOfColumn 
   //find footer element of column 
   //set its text content to the sum 
};

const isNumber = function(el) {
    //determine if element is a number
}

const getColumnValues = function(column) {
  //input: target column to find sum of 
    //iterate through all rows in target column 
      //determine if element in row isNumber
        //if it is a number add it to a collection of numbers (array)
  //output: array of all numeric column values 

};

const calculateSumOfColumn= function(array) {
  //iterate through array 
    //add all elements 
  //output: sum 
};