const renderColumnSums = function(colNum) {
  // pull value of all cells in a column into `values` array
  // pass `values` array into addValues
  // find footer element of column
  // set textContent = sum
};

const addValues = function(values) {
  // for each el, pass el to `isNumeric` to determine if numeric
  // if so, add to running sum
  // return sum
};

const isNumeric = function(value) {
  // if parseInt(value) !== NaN, return true
};

