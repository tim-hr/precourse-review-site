const renderColumnSums = function() {
  // create element to which sum will be added
  // get column values array
  // pass column values array into computeSum function
  // modify innerHTML of element with value returned from computeSum
};

const getColumnValues = function(columnNumber) {
  // instantiate an empty array
  // loop through rows of respective columnNumber
  // push filtered values into array if not NaN
  // return array
};

const computeSum = function(numberArray) {
  // fiter non-numeric values in array
  // reduce numeric array values into sum
};
