// The code I wrote below nukes the first data row automatically
// I'm not sure how to connect that event to the button

// let headerRow = document.querySelector('THEAD TR');

// const remove = function(el) {
//   let child = headerRow;
//   child.parentNode.removeChild(child);
// };

// remove(headerRow);