const renderColumnSums = function() {
  // for each col index upto the num of cols
  // fetch column values of the cells using the helper fetchColumnValues
  // compute the sum of the filtered list of column cell values using computeColumnSum 
  // find the footer element for this column
  // set its textcontent to the sum
};

const fetchColumnValues = function(columnIndex) {
  // fetch all cell values for this column index
  // filter out non-numeric values from this list
};

const computeColumnSum = function(listOfCellValues) {
  // add up the values in the argument list and return the sum
};