const renderColumnSums = function() {
  // find the footer element for column index
  // set its text content to the sum found in addCellValues
};

const findCellValues = function() {
  // fetch all the cell values for that column from the model
  //  use a for-loop to iterate through the column
  //  use !isNaN to filter out the non-numeric values from the column list
};

const addCellValues = function() {
  // add up the filtered values in thee list
  //  write an add formula
  //  use findCellValues to catch the numeric values
  //  implement findCellValues to add formula
};