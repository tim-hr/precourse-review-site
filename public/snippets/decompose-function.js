const renderColumnSums = function() {
  // call renderFooterElements
};

const calculateColumnSum = function(column) {
  // fetch all the cell values for column from the model
  // filter out the non-numeric values from this list
  // add up the filtered values in the list
  //   return the sum of the filtered values
};

const renderFooterElements = function() {
  // for each col index up to the num of cols
     // find the footer element for this column
     // set its text content to calculateColumnSum(column)
  
};