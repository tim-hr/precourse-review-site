const renderColumnSums = function() {
  // for each col index up to num of cols
  //    set its text content to findSum(getColNumNumbers)
};

const getColNumbers = function(column) {
  // create an empty array to collect numbers in a column
  // for each item in column
  //    add to the newly created array if the item is a number
};

const findSum = function(numbers) {
  // expect argument to be an array of numbers
  // return the sum via reducing numbers
};