const renderColumnSums = function() {
  // for each col index up to the num of cols
    // call helper function to sum column
    // pass sum for column to helper function to print in footer
};

const sumColumn = function(col) {
  // fetch all the cell values in the model corresponding to the col argument
  // filter out non-numeric values from list
  // add up filtered values in the list and return
};

const renderSum = function(sum) {
  // find the footer element for this column
  // set its text content to the sum argument
}; 
