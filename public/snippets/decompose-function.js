const renderColumnSums = function() {
  // find the footer element for the column
  // set its text content to the sum
};

const calulateColumnSums = function() {
  // add up the filtered values in the list
};

const getColumnValues = function() {
  // for each column index up to the number of columns
  //   fetch all of the cell values for that column from the model
  //   filter out the non-numeric values from this list
};