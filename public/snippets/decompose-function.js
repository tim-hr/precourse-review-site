const renderColumnSums = function() {
  // for each column to the number of columns
  //   find the footer element for the col 
  //   set its value to addColumnValues(col).
};

const addColumnValues = function(col) {
  // create a total variable
  // for each row in the column 
  //   get the value in the cell
  //   call filterColumnValue(val)
  //   add the resulting value to the total
};

const filterColumnValue = function(val) {
  // use Number parseInt on value
  // return the resulting int or return 0 when parseInt returns NaN.
  
};