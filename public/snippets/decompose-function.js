const renderColumnSums = function() {
  // for each column index up to the number of columns
  //   get the sum of that column 
  //   set that column footer's value to the sum
};

const getSumOfColumn = function(col) {
  // fetch all the cell values for col from the model
  // filter out the non-numeric values from this list
  // add up the filtered values in the list
  // return the sum
};

const setColumnFooter = function(col, sum) {
  // find the footer element for col
  // set its text content to sum
};