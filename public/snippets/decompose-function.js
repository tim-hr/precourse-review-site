const renderColumnSums = function() {
  // for each col index up to the num of cols
  //    computeColumnSum()
  //    renderTableFooter()
};

const computeColumnSum = function() {
  // fetch all the cell values for the column from the model
  // filter out non-numeric values from this list
  // add up the filtered values in the list
};

const renderTableFooter = function() {
  // find the footer element for this column
  // set its text content to the sum
};