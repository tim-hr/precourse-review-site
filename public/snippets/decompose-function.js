const renderColumnSums = function() {
  // for each col index up to num cols:
  //   assign return from getColumnVals to columnVals var
  //   assign return from sumVals(columnVals) to sum var
  //   find footer element for current column and set its text content to sum
};

const getColumnVals = function(col) {
  // create empty columnVals array, and for each row in col, extract value and push into the array
  // return columnVals
};

const sumVals = function(columnVals) {
  // filter columnVals for numeric values
  // create sum var and loop through filtered array; add values to sum
  // return sum
};
