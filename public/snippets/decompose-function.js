const renderColumnSums = function() {
  // for each col index up to the total number of cols
    // call getColumnSum
    // find the footer element for the column
    // set its text content to the sum
};

const getColumnSum = function(col) {
  // assign a list to filterColumnNumbers(col)
  // add up all the values from the list
  // return the sum
};

const filterColumnNumbers = function(col) {
  // fetch all the cell values for column col from the model into a list
  // filter out the non-numeric values from the list
  // return the filtered list
};